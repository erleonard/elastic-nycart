{
    "$schema": "https://schema.management.azure.com/schemas/2019-04-01/deploymentParameters.json#",
    "contentVersion": "1.0.0.0",
    "parameters": {
      "environmentName": {
        "value": "${AZURE_ENV_NAME}"
      },
      "location": {
        "value": "${AZURE_LOCATION}"
      },
      "ELASTIC_SEARCH_INDEX": {
        "value": "${ELASTIC_SEARCH_INDEX}"
      },
      "ELASTIC_VECTOR_FIELD": {
        "value": "${ELASTIC_VECTOR_FIELD}"
      },
      "ELASTIC_EMBEDDING_MODEL_ID": {
        "value": "${ELASTIC_EMBEDDING_MODEL_ID}"
      },
      "ELASTIC_COMPLETION_MODEL_ID": {
        "value": "${ELASTIC_COMPLETION_MODEL_ID}"
      },
      "ELASTIC_INFERENCE_PIPELINE_MODEL_ID": {
        "value": "${ELASTIC_INFERENCE_PIPELINE_MODEL_ID}"
      },
      "AZURE_EMBEDDING_API_VERSION": {
        "value": "${AZURE_EMBEDDING_API_VERSION}"
      },
      "AZURE_COMPLETION_API_VERSION": {
        "value": "${AZURE_COMPLETION_API_VERSION}"
      },
      "ELASTIC_CLUSTER": {
        "value": "${ELASTIC_CLUSTER}"
      },
      "ELASTIC_API_KEY": {
        "value": "${ELASTIC_API_KEY}"
      },
      "ENDPOINT_BASE": {
        "value": "${ENDPOINT_BASE}"
      },
      "ENDPOINT": {
        "value": "${ENDPOINT}"
      },
      "AZURE_API_KEY": {
        "value": "${AZURE_API_KEY}"
      },
      "AZURE_EMBEDDING_RESOURCE_NAME": {
        "value": "${AZURE_EMBEDDING_RESOURCE_NAME}"
      },
      "AZURE_EMBEDDING_DEPLOYMENT_ID": {
        "value": "${AZURE_EMBEDDING_DEPLOYMENT_ID}"
      },
      "AZURE_COMPLETION_RESOURCE_NAME": {
        "value": "${AZURE_COMPLETION_RESOURCE_NAME}"
      },
      "AZURE_COMPLETION_DEPLOYMENT_ID": {
        "value": "${AZURE_COMPLETION_DEPLOYMENT_ID}"
      },
      "ASSISTANT_ID": {
        "value": "${ASSISTANT_ID}"
      }
    }
}